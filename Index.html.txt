<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Kesav Portfolio</title>

<style>
body {
  margin: 0;
  font-family: Arial, sans-serif;
  color: #eaeaea;
  overflow-x: hidden;

  /* 🌈 minimal aesthetic background */
  background: linear-gradient(135deg, #0b0c10, #111827, #0a0f1f);
}

/* 3D canvas */
canvas {
  position: fixed;
  top: 0;
  left: 0;
  z-index: 0;
}

/* NAV BAR */
nav {
  position: fixed;
  top: 0;
  width: 100%;
  display: flex;
  justify-content: center;
  gap: 12px;
  padding: 15px;
  z-index: 10;

  background: rgba(255,255,255,0.03);
  backdrop-filter: blur(12px);
}

nav button {
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(123, 220, 207, 0.4);
  color: #7bdccf;
  padding: 8px 14px;
  border-radius: 10px;
  cursor: pointer;
  transition: 0.3s;
}

nav button:hover {
  background: rgba(123, 220, 207, 0.15);
}

nav button.active {
  background: rgba(123, 220, 207, 0.25);
  color: white;
}

/* SECTIONS */
.section {
  display: none;
  padding: 120px 20px 60px;
  text-align: center;
  position: relative;
  z-index: 2;
}

.section.active {
  display: block;
}

/* GLASS CARD */
.card {
  max-width: 750px;
  margin: auto;
  padding: 30px;
  border-radius: 18px;

  background: rgba(255,255,255,0.05);
  backdrop-filter: blur(15px);
  border: 1px solid rgba(255,255,255,0.1);

  box-shadow: 0 0 30px rgba(123,220,207,0.1);
}

/* TITLE */
h1 {
  font-size: 55px;
  color: #7bdccf;
  text-shadow: 0 0 20px #7bdccf;
}

/* MOTIVATION */
.motivation {
  font-size: 16px;
  opacity: 0.85;
  line-height: 1.7;
}

/* GALLERY */
.grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 10px;
  margin-top: 20px;
}

.grid img, .grid video {
  width: 160px;
  border-radius: 10px;
}

/* MOBILE */
@media (max-width:600px){
  h1 { font-size: 38px; }
}
</style>
</head>

<body>

<!-- NAV -->
<nav>
  <button class="active" onclick="show(0)">Home</button>
  <button onclick="show(1)">Myself</button>
  <button onclick="show(2)">Photos</button>
  <button onclick="show(3)">Links</button>
</nav>

<!-- HOME -->
<div class="section active">
  <div class="card">
    <h1>KESAV</h1>

    <p class="motivation">
      “Success is built daily, not overnight.”<br><br>

      You are not just studying — you are building your future.  
      Every concept you learn, every problem you solve,  
      is shaping a stronger version of you. 🚀<br><br>

      Stay consistent. Stay curious. Never give up.
    </p>
  </div>
</div>

<!-- MYSELF -->
<div class="section">
  <div class="card">
    <h2>About Me</h2>
    <p>
      I am <b>Kesav</b>, an Inter 2nd Year MPC student.  
      I love Physics, Chemistry, and understanding how the universe works.  
      I enjoy building creative websites and exploring technology.
    </p>
  </div>
</div>

<!-- PHOTOS & VIDEOS -->
<div class="section">
  <div class="card">
    <h2>Photos & Videos</h2>
    <input type="file" multiple onchange="upload(event)">
    <div class="grid" id="gallery"></div>
  </div>
</div>

<!-- LINKS -->
<div class="section">
  <div class="card">
    <h2>Links</h2>
    <p>Email: kesav@example.com</p>
  </div>
</div>

<!-- THREE JS -->
<script src="https://cdn.jsdelivr.net/npm/three@0.158.0/build/three.min.js"></script>

<script>

/* NAV SYSTEM */
function show(i){
  document.querySelectorAll(".section").forEach((s,idx)=>{
    s.classList.toggle("active", idx===i);
  });

  document.querySelectorAll("nav button").forEach((b,idx)=>{
    b.classList.toggle("active", idx===i);
  });
}

/* GALLERY UPLOAD */
function upload(e){
  const box = document.getElementById("gallery");

  Array.from(e.target.files).forEach(file=>{
    const url = URL.createObjectURL(file);

    if(file.type.startsWith("image")){
      const img = document.createElement("img");
      img.src = url;
      box.appendChild(img);
    } else {
      const vid = document.createElement("video");
      vid.src = url;
      vid.controls = true;
      box.appendChild(vid);
    }
  });
}

/* 3D SCENE */
const scene = new THREE.Scene();

const camera = new THREE.PerspectiveCamera(50, window.innerWidth/window.innerHeight, 0.1, 100);
camera.position.z = 6;

const renderer = new THREE.WebGLRenderer({ alpha:true, antialias:true });
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

/* CORE OBJECT */
const core = new THREE.Mesh(
  new THREE.IcosahedronGeometry(1.3,1),
  new THREE.MeshBasicMaterial({ color:0x7bdccf, wireframe:true })
);
scene.add(core);

/* STARS */
for(let i=0;i<180;i++){
  const star = new THREE.Mesh(
    new THREE.SphereGeometry(0.02,6,6),
    new THREE.MeshBasicMaterial({ color:0xffffff })
  );

  star.position.x = (Math.random()-0.5)*30;
  star.position.y = (Math.random()-0.5)*20;
  star.position.z = (Math.random()-0.5)*20;

  scene.add(star);
}

/* ANIMATION */
function animate(){
  requestAnimationFrame(animate);

  core.rotation.y += 0.004;
  core.rotation.x += 0.002;

  renderer.render(scene,camera);
}
animate();

/* RESIZE */
window.addEventListener("resize",()=>{
  camera.aspect = window.innerWidth/window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

</script>

</body>
</html>